import * as types from './mutation-types'
export const initPage = ({commit}, {data}) => {
    // return api.getUserInfo({username}).then(data => {
    //     commit(types.INIT_USERINFO, {
    //         data
    //     })
    //     return data
    // })
}